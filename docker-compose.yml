services:
  goldprice-bot:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8080:8080"
    env_file:
      - .env
    environment:
      - DB_PATH=/app/data/database.db
    volumes:
      - ./data:/app/data
    restart: unless-stopped
